import { AppRegistry } from 'react-native';
import { App } from '@/App';

// Registered under "SchoolManagement"; match this in the native project's
// MainActivity / AppDelegate when generating the platform folders.
AppRegistry.registerComponent('SchoolManagement', () => App);
